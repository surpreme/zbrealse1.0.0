<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aite.aitezhongbao">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".app.App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.WelcomeActivity" />
        <activity android:name=".activity.NewUserActivity" />
        <activity android:name=".activity.NewUserMsgActivity" />
        <activity android:name=".activity.UserTypeActivity" />
        <activity android:name=".activity.FindKeyActivity" />
        <activity android:name="com.aite.mainlibrary.TabyActivity" />
        <activity android:name=".activity.login.LoginActivity" />

    </application>

</manifest>